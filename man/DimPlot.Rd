% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{DimPlot}
\alias{DimPlot}
\title{DimPlot}
\usage{
DimPlot(
  db_file,
  reduction = "umap",
  group.by = NULL,
  shape.by = NULL,
  shuffle = FALSE,
  raster = NULL,
  raster.dpi = c(512, 512),
  group.by.table = "counts",
  log = FALSE,
  ...
)
}
\arguments{
\item{db_file}{A database db_filenection object.}

\item{reduction}{A string specifying the type of dimensionality reduction to use (default is "umap").}

\item{group.by}{An optional string specifying the column to group points by.}

\item{shape.by}{An optional string specifying the column to shape points by.}

\item{shuffle}{A logical value indicating whether to shuffle the points (default is TRUE).}

\item{raster}{A logical value indicating whether to use rasterization for large datasets (default is NULL).}

\item{raster.dpi}{A numeric vector specifying the resolution for rasterization (default is c(512, 512)).}

\item{group.by.table}{A string specifying the table to use for grouping (default is "counts").}

\item{log}{A logical value indicating whether to log-transform the data (default is TRUE).}

\item{...}{Additional arguments passed to the plotting functions.}
}
\value{
A ggplot object representing the dimensionality reduction plot.
}
\description{
Generates a dimensionality reduction plot (e.g., UMAP, PCA) from a database db_filenection.
}
\details{
This function reads the dimensionality reduction data from the specified table in the database,
db_filestructs a ggplot object with the specified aesthetics, and optionally applies rasterization
for large datasets to improve performance.
}
\examples{
# Example usage:
# Generate a UMAP plot grouped by "cell_type":
# DimPlot(db_file = db_file, reduction = "umap", group.by = "cell_type")

# Generate a PCA plot with points shaped by "condition" and grouped by "cell_type":
# DimPlot(db_file = db_file, reduction = "pca", group.by = "cell_type", shape.by = "condition")

# Generate a UMAP plot with rasterization for large datasets:
# DimPlot(db_file = db_file, reduction = "umap", raster = TRUE, raster.dpi = c(300, 300))

}
